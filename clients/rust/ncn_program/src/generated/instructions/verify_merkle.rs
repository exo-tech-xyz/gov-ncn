//! This code was AUTOGENERATED using the kinobi library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun kinobi to update it.
//!
//! <https://github.com/kinobi-so/kinobi>
//!

use crate::generated::types::MetaMerkleLeaf;
use crate::generated::types::StakeMerkleLeaf;
use borsh::BorshDeserialize;
use borsh::BorshSerialize;

/// Accounts.
pub struct VerifyMerkle {
    pub consensus_result: solana_program::pubkey::Pubkey,
}

impl VerifyMerkle {
    pub fn instruction(
        &self,
        args: VerifyMerkleInstructionArgs,
    ) -> solana_program::instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: VerifyMerkleInstructionArgs,
        remaining_accounts: &[solana_program::instruction::AccountMeta],
    ) -> solana_program::instruction::Instruction {
        let mut accounts = Vec::with_capacity(1 + remaining_accounts.len());
        accounts.push(solana_program::instruction::AccountMeta::new_readonly(
            self.consensus_result,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = VerifyMerkleInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_program::instruction::Instruction {
            program_id: crate::NCN_PROGRAM_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshDeserialize, BorshSerialize)]
pub struct VerifyMerkleInstructionData {
    discriminator: u8,
}

impl VerifyMerkleInstructionData {
    pub fn new() -> Self {
        Self { discriminator: 31 }
    }
}

impl Default for VerifyMerkleInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct VerifyMerkleInstructionArgs {
    pub meta_merkle_proof: Vec<[u8; 32]>,
    pub meta_merkle_leaf: MetaMerkleLeaf,
    pub stake_merkle_proof: Option<Vec<[u8; 32]>>,
    pub stake_merkle_leaf: Option<StakeMerkleLeaf>,
}

/// Instruction builder for `VerifyMerkle`.
///
/// ### Accounts:
///
///   0. `[]` consensus_result
#[derive(Clone, Debug, Default)]
pub struct VerifyMerkleBuilder {
    consensus_result: Option<solana_program::pubkey::Pubkey>,
    meta_merkle_proof: Option<Vec<[u8; 32]>>,
    meta_merkle_leaf: Option<MetaMerkleLeaf>,
    stake_merkle_proof: Option<Vec<[u8; 32]>>,
    stake_merkle_leaf: Option<StakeMerkleLeaf>,
    __remaining_accounts: Vec<solana_program::instruction::AccountMeta>,
}

impl VerifyMerkleBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn consensus_result(
        &mut self,
        consensus_result: solana_program::pubkey::Pubkey,
    ) -> &mut Self {
        self.consensus_result = Some(consensus_result);
        self
    }
    #[inline(always)]
    pub fn meta_merkle_proof(&mut self, meta_merkle_proof: Vec<[u8; 32]>) -> &mut Self {
        self.meta_merkle_proof = Some(meta_merkle_proof);
        self
    }
    #[inline(always)]
    pub fn meta_merkle_leaf(&mut self, meta_merkle_leaf: MetaMerkleLeaf) -> &mut Self {
        self.meta_merkle_leaf = Some(meta_merkle_leaf);
        self
    }
    /// `[optional argument]`
    #[inline(always)]
    pub fn stake_merkle_proof(&mut self, stake_merkle_proof: Vec<[u8; 32]>) -> &mut Self {
        self.stake_merkle_proof = Some(stake_merkle_proof);
        self
    }
    /// `[optional argument]`
    #[inline(always)]
    pub fn stake_merkle_leaf(&mut self, stake_merkle_leaf: StakeMerkleLeaf) -> &mut Self {
        self.stake_merkle_leaf = Some(stake_merkle_leaf);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: solana_program::instruction::AccountMeta,
    ) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_program::instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_program::instruction::Instruction {
        let accounts = VerifyMerkle {
            consensus_result: self.consensus_result.expect("consensus_result is not set"),
        };
        let args = VerifyMerkleInstructionArgs {
            meta_merkle_proof: self
                .meta_merkle_proof
                .clone()
                .expect("meta_merkle_proof is not set"),
            meta_merkle_leaf: self
                .meta_merkle_leaf
                .clone()
                .expect("meta_merkle_leaf is not set"),
            stake_merkle_proof: self.stake_merkle_proof.clone(),
            stake_merkle_leaf: self.stake_merkle_leaf.clone(),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `verify_merkle` CPI accounts.
pub struct VerifyMerkleCpiAccounts<'a, 'b> {
    pub consensus_result: &'b solana_program::account_info::AccountInfo<'a>,
}

/// `verify_merkle` CPI instruction.
pub struct VerifyMerkleCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_program::account_info::AccountInfo<'a>,

    pub consensus_result: &'b solana_program::account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: VerifyMerkleInstructionArgs,
}

impl<'a, 'b> VerifyMerkleCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_program::account_info::AccountInfo<'a>,
        accounts: VerifyMerkleCpiAccounts<'a, 'b>,
        args: VerifyMerkleInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            consensus_result: accounts.consensus_result,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(
        &self,
        signers_seeds: &[&[&[u8]]],
    ) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> solana_program::entrypoint::ProgramResult {
        let mut accounts = Vec::with_capacity(1 + remaining_accounts.len());
        accounts.push(solana_program::instruction::AccountMeta::new_readonly(
            *self.consensus_result.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_program::instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = VerifyMerkleInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_program::instruction::Instruction {
            program_id: crate::NCN_PROGRAM_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(1 + 1 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.consensus_result.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_program::program::invoke(&instruction, &account_infos)
        } else {
            solana_program::program::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `VerifyMerkle` via CPI.
///
/// ### Accounts:
///
///   0. `[]` consensus_result
#[derive(Clone, Debug)]
pub struct VerifyMerkleCpiBuilder<'a, 'b> {
    instruction: Box<VerifyMerkleCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> VerifyMerkleCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_program::account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(VerifyMerkleCpiBuilderInstruction {
            __program: program,
            consensus_result: None,
            meta_merkle_proof: None,
            meta_merkle_leaf: None,
            stake_merkle_proof: None,
            stake_merkle_leaf: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn consensus_result(
        &mut self,
        consensus_result: &'b solana_program::account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.consensus_result = Some(consensus_result);
        self
    }
    #[inline(always)]
    pub fn meta_merkle_proof(&mut self, meta_merkle_proof: Vec<[u8; 32]>) -> &mut Self {
        self.instruction.meta_merkle_proof = Some(meta_merkle_proof);
        self
    }
    #[inline(always)]
    pub fn meta_merkle_leaf(&mut self, meta_merkle_leaf: MetaMerkleLeaf) -> &mut Self {
        self.instruction.meta_merkle_leaf = Some(meta_merkle_leaf);
        self
    }
    /// `[optional argument]`
    #[inline(always)]
    pub fn stake_merkle_proof(&mut self, stake_merkle_proof: Vec<[u8; 32]>) -> &mut Self {
        self.instruction.stake_merkle_proof = Some(stake_merkle_proof);
        self
    }
    /// `[optional argument]`
    #[inline(always)]
    pub fn stake_merkle_leaf(&mut self, stake_merkle_leaf: StakeMerkleLeaf) -> &mut Self {
        self.instruction.stake_merkle_leaf = Some(stake_merkle_leaf);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_program::account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(
        &self,
        signers_seeds: &[&[&[u8]]],
    ) -> solana_program::entrypoint::ProgramResult {
        let args = VerifyMerkleInstructionArgs {
            meta_merkle_proof: self
                .instruction
                .meta_merkle_proof
                .clone()
                .expect("meta_merkle_proof is not set"),
            meta_merkle_leaf: self
                .instruction
                .meta_merkle_leaf
                .clone()
                .expect("meta_merkle_leaf is not set"),
            stake_merkle_proof: self.instruction.stake_merkle_proof.clone(),
            stake_merkle_leaf: self.instruction.stake_merkle_leaf.clone(),
        };
        let instruction = VerifyMerkleCpi {
            __program: self.instruction.__program,

            consensus_result: self
                .instruction
                .consensus_result
                .expect("consensus_result is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct VerifyMerkleCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_program::account_info::AccountInfo<'a>,
    consensus_result: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    meta_merkle_proof: Option<Vec<[u8; 32]>>,
    meta_merkle_leaf: Option<MetaMerkleLeaf>,
    stake_merkle_proof: Option<Vec<[u8; 32]>>,
    stake_merkle_leaf: Option<StakeMerkleLeaf>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(
        &'b solana_program::account_info::AccountInfo<'a>,
        bool,
        bool,
    )>,
}
